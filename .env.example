# Base URL of the Cirkle backend (the one that serves /organizer/*).
# In development this is the founder's ngrok tunnel URL; it changes whenever
# the tunnel restarts. No trailing slash.
VITE_API_URL=https://your-ngrok-subdomain.ngrok-free.app
